// Minimal auth module stub to satisfy imports in main.js
export function setupAuthListeners() {
  console.log('setupAuthListeners() called (stub)');
}

export function setupAuthForms(openModal, closeModal, createSite, selectSite, getUserSites) {
  console.log('setupAuthForms() called (stub)');
  // No-op: real implementation would bind form submit handlers
}
